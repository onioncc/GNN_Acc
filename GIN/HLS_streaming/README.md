This version uses streaming between different nodes, the MLP\_PE for a single node.
The kernal time is 160ms; good but not scalable.
Still need to parallize the dimensions.
Can be saved for future reference.
